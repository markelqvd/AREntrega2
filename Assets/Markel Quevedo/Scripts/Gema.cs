using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gemaa : MonoBehaviour
{
    /*public delegate void GemaRecogidaHandler();
    public static event GemaRecogidaHandler OnGemaRecogida;

    private void OnTriggerEnter(Collider other)
    {
            // Avisar que se recogió la gema
            OnGemaRecogida?.Invoke();

            // Destruir la gema
            Destroy(gameObject);
        
    }*/
}
